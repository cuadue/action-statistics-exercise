# `ActionStatistics` Python Library Class Exercise

This Python module provides the `ActionStatistics` class which maintains the
average time for actions.

Requires Python3.

For example usage, see `example_action_statistics.py`.
